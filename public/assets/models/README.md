# Modelos 3D gerados

Os arquivos `.glb` usados pelo jogo são gerados a partir de `src/game/art-models.ts` pelo script `npm run models`.

Eles não são versionados para evitar binários duplicados no Git. `npm run dev` e `npm run build` executam a geração automaticamente antes de iniciar/compilar.
